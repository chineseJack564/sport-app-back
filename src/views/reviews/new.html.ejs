<div>
    <% if (locals.errors) { %>
        <div class="errors">
            You need to fix the following errors:
            <ul>
                <% errors.forEach(error => { %>
                    <li><%= error.message %></li>
                <% });%>
            </ul>
        </div>
    <% } %>

    <div class="form-box" id='new_exercise'>
        <form action='<%= submitPath %>' method="POST">
            <div class="form-title">
                <h2 class='new-form-title'>New Review</h2>
            </div>

            <div class="input-box" id="new-form-input-box">
                <div class="label-input-box">
                    <label for="title">Title</label>
                    <input type="text" class="register-form-input" name="title" id="title"/>
                </div>

                <div class="label-input-box">
                    <label for="score">Score</label>
                    <input type="number" class="register-form-input" name="score" id="score"/>
                </div>

                <div class="label-input-box">
                    <label for="content">Content</label>
                    <textarea name="content" id="content" rows="4" cols="50" class="register-form-input-area"> </textarea>
                </div>
            </div>

            <input type="submit" class="form-submit-button" id="new-form-button" value="Create Review">
        </form>

        <!-- <a href='<%= reviewPath %>'>Review Index</a> -->
    </div>
</div>